# A aprovação do script de instalação do esbuild NÃO mora mais aqui: o pnpm 11
# só lê as opções próprias dele do pnpm-workspace.yaml, e a chave é
# `allowBuilds`. Ver o arquivo ao lado.
#
# Este arquivo fica para as opções que ainda são de npmrc — e como lembrete de
# que a chave antiga, `only-built-dependencies[]`, não era lida por ninguém.
